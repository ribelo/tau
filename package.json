{
  "name": "tau",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "check": "tsgo --noEmit",
    "lint": "oxlint .",
    "lint:fix": "oxlint . --fix",
    "format": "oxfmt --write .",
    "format:check": "oxfmt --check .",
    "gate": "bun run check && bun run lint && bun run format:check"
  },
  "devDependencies": {
    "@effect/language-service": "^0.77.0",
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
