{"schema_version":1,"event_id":"update-bb4c2cca-0b0f-48c3-acbe-3b7d59669927","issue_id":"tau-vaiq","recorded_at":"2026-04-22T01:00:02.193Z","actor":"backlog","kind":"issue.updated","set_fields":{"comments":[{"id":1,"issue_id":"tau-vaiq","author":"backlog","text":"Verified in current checkout. AgentWorker constructs WorkerSessionController with spawnBackground: (effect) => Effect.runFork(effect) at extensions/tau/src/agent/worker.ts:72-83, so worker background tasks are launched on the default runtime rather than a worker/extension-owned scope. WorkerSessionController.attach() triggers reprompts via this.options.spawnBackground(this.replaceBackground(this.options.repromptForSubmitResult(retry))) at extensions/tau/src/agent/worker/session-controller.ts:40-43. replaceBackground(...) only stores the inner fiber created inside its effect (line 57). The outer detached fiber that runs replaceBackground(...) is never tracked. shutdown() only interrupts activeFiber (lines 63-77), so shutdown can miss that outer fiber if it has not yet installed the inner active fiber. That creates a real lifecycle race: a late outer fiber can still spawn a reprompt after shutdown/release. The broader structured-concurrency concern is also valid because runFork is detached from the worker lifecycle.","created_at":"2026-04-22T00:50:12.365Z"},{"id":2,"issue_id":"tau-vaiq","author":"backlog","text":"Related architectural verification: the worker split is helpful but incomplete. Lifecycle ownership still leaks through AgentWorker via spawnBackground: (effect) => Effect.runFork(effect) in extensions/tau/src/agent/worker.ts:72-83, so WorkerSessionController is not the sole owner of worker background resources. Session/event state also still mutates a shared mutable WorkerTrackingState object directly from subscription callbacks in extensions/tau/src/agent/worker/session-subscription.ts:71-148, rather than being encapsulated behind a scoped service/resource boundary. This is not a separate compile break, but it supports the broader point that file decomposition did not fully become ownership decomposition.","created_at":"2026-04-22T01:00:02.193Z"}],"updated_at":"2026-04-22T01:00:02.193Z"},"unset_fields":[]}
