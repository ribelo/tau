{"schema_version":1,"event_id":"update-874694fc-a2b0-46bb-b3e5-504cb86fcdba","issue_id":"tau-hkpr","recorded_at":"2026-04-22T01:43:24.525Z","actor":"backlog","kind":"issue.updated","set_fields":{"comments":[{"id":1,"issue_id":"tau-hkpr","author":"backlog","text":"Verified in current checkout with one path correction. The checker lives at extensions/tau/scripts/check-runfork-usage.mjs, not repo-root scripts/. It hardcodes ALLOWED_FILES = { 'src/app.ts', 'src/agent/worker.ts', 'src/services/footer.ts' } (lines 4-8), so src/agent/worker.ts remains explicitly allowlisted even though that area has the worker lifecycle/runFork bug tracked separately. Detection is also literal substring matching only: line 42 checks line.includes('Effect.runFork('). That means aliases, imported renames, bracket access, wrapped helpers, or non-literal forms bypass the checker. Current tau source already mostly routes runFork through runtime adapters (e.g. worker.ts uses this.runFork(effect) rather than a direct literal), so the checker currently protects only direct textual Effect.runFork calls outside the allowlist. This is a real weakness in the guardrail, though it is a tooling/design issue rather than a runtime bug.","created_at":"2026-04-22T01:04:30.221Z"},{"id":2,"issue_id":"tau-hkpr","author":"backlog","text":"Implemented AST-based runFork checker in `extensions/tau/scripts/check-runfork-usage.mjs` using the TypeScript parser instead of line substring matching. The checker now detects Effect runFork calls through: (1) aliased Effect imports (`import { Effect as Fx }` + `Fx.runFork(...)`), (2) bracket member access (`Effect[\"runFork\"](...)`), and (3) local aliases assigned from runFork (`const fork = Effect.runFork; fork(...)`) including `import { runFork }` aliasing. Also tightened allowlist from multiple files to only `src/app.ts` (removed `src/agent/worker.ts` and `src/services/footer.ts`). Added regression coverage in `extensions/tau/test/check-runfork-usage.test.ts` with fixture-based command execution against temporary workspaces, including a guard that direct usage in `src/agent/worker.ts` is now rejected while `src/app.ts` remains allowed. Validation: `npm run gate` passed.","created_at":"2026-04-22T01:43:24.525Z"}],"updated_at":"2026-04-22T01:43:24.525Z"},"unset_fields":[]}
